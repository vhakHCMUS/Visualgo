#pragma once

#include "TextBox.h"

void MenuQueue(sf::RenderWindow& window, sf::Font& font, bool& MenuCur, bool& Queue, TextBox& textField, Buttons& visual, TextBox& addField, doublyLinkedList& doublyLL, bool& init, bool& add);

